# Exercise 7: Comparative Study of the Lennard-Jones Model using MD (NVE) and MCMC (NVT) Simulations

This document provides the necessary guidelines for executing and analyzing the code developed for the seventh assignment of the **Computational Physics Laboratory (LSN)** course.

## Prerequisites

The **`NSL_SIMULATOR`** code is dependent on the **Armadillo C++** linear algebra library. Ensure this library is correctly **installed and linked** on your system before proceeding with compilation.

---

## 0. Introduction

This project aims to simulate the liquid phase (with optional extensions to the gas and solid phases) of the Lennard-Jones model using two distinct statistical ensembles: the **Canonical (NVT) ensemble** via **MCMC algorithms** and the **Microcanonical (NVE) ensemble** via **Molecular Dynamics (MD)**. Both simulations are conducted under the same thermodynamic conditions. The primary objective is to compare the ensemble-averaged properties produced by the two simlations, thereby confirming the validity of statistical mechanics as an effective framework for studying complex systems.

The following routines are implemented in this project:

- A routine for calculating **tail corrections** to the potential energy and pressure, which accounts for the spherical cutoff approximation of the potential.

- A routine for the **autocorrelation function analysis**, used to determine the minimum block size required to satisfy the statistical independence requirement for reliable error estimation via the blocking method

- A routine for **sampling the Radial Distribution Function** $g(r)$.

---

## 1. Project Structure and Data Flow

The project is organized within the **`LSN_07`** working directory. It contains subdirectories (`MC_NVT` and `MD_NVE`) for all input/output data, two **Jupyter Notebooks** (one for the exercise requirements, one for data analysis), the simulator code (`NSL_SIMULATOR`) and one subdirectory for aggregated plots (`plot`).

| Directory/File | Description |
| :--- | :--- |
| **`LSN_Exercises_07.ipynb`** | **Jupyter Notebook** containing the exercise requests. |
| **`LSN_Notebook_7.ipynb`** | **Jupyter Notebook** dedicated to **data analysis and plotting**. |
| **`NSL_SIMULATOR/`** | The core **simulator** (source code and binaries). |
| **`MC_NVT`** and **`MD_NVE`** directories | Subdirectories containing all **input and output files** organized by the three simulated matter phases (`gas`, `liquid`, `solid`). |
| `plot/` | Directory for saving final, aggregated plots generated by the analysis notebook. |

```
LSN_07
.
├── LSN_Exercises_07.ipynb
├── LSN_Notebook_7.ipynb                # Jupyter Notebook for data analysis.
├── MC_NVT                              # MCMC archive directory
│   ├── gas                             # Gas phase archive
│   │   ├── equilibration               # Equilibration run files archive
│   │   └── run                         # Analysis run files archive
│   ├── liquid                          # Liquid phase archive
│   │   ├── autocorrelation_run         # Autocorrelation run files archive
│   │   ├── equilibration               
│   │   ├── run                         # Final run files archive
│   │   └── tail_corrections            # Tail corrections files archive
│   │       ├── off
│   │       └── on
│   └── solid                           # Solid phase archive
│       ├── equilibration
│       └── run
├── MD_NVE                              # MD archive directory
│   ├── gas
│   │   ├── equilibration
│   │   └── run
│   ├── liquid
│   │   ├── autocorrelation_run
│   │   ├── equilibration
│   │   ├── run
│   │   └── tail_corrections
│   │       ├── off
│   │       └── on
│   └── solid
│       ├── equilibration
│       └── run
├── NSL_SIMULATOR                       # Simulator code
│   ├── INPUT
│   ├── OUTPUT
│   └── SOURCE
├── ReadMe.md
├── images
└── plot
    ├── gas
    │   ├── equilibration
    │   └── run
    ├── liquid
    │   ├── autocorrelation
    │   ├── equilibration
    │   ├── run
    │   └── tail_corrections
    └── solid
        ├── equilibration
        └── run
```

**Note**: Sub-directories INPUT/OUTPUT and files within them are omitted for brevity.

### Execution Workflow

The analysis notebook (`LSN_Notebook_7.ipynb`) processes data from the `MD_NVE/*/equilibration/OUTPUT`,`MD_NVE/*/autocorrelation_run/OUTPUT`, `MD_NVE/*/run/OUTPUT`, `MC_NVT/*/equilibration/OUTPUT`,`MC_NVT/*/autocorrelation_run/OUTPUT`, `MC_NVT/*/run/OUTPUT` directories. Therefore, **all simulations must be successfully executed** before proceeding to the final analysis step.

---

## 2. Configuration File Structure

### `input.dat`: Simulation Parameters

This file contains configuration parameters necessary to set up the simulation.

| Parameter | Possible Value | Description/Notes |
| :--- | :--- | :--- |
| `SIMULATION_TYPE` | $0/1$ | $0$: LJ NVE MD; $1$: LJ NVE MC (M(RT)^2) |
| `DISTRIBUTION_TYPE` | $0$ | **Only for LJ NVE MD simulations** |
| `RESTART` | $0/1$ | $0$: Start from initial configuration; $1$: Restart from previous configuration files. |
| `TEMP` | $1.2-1./1.1-1./0.8-1.$ | Temperature $T$. NVT simulation uses `TEMP` $= 1.2/1.1/0.8$, NVE must start from `TEMP` $= 1./1.972/1.$ to reach the target NVT temperature after equilibration. |
| `NPART` | $108$ | **Do not change** (critical for initialization). |
| `RHO` | $0.05/0.8/1.1$ | The reduced density $\rho^*$. |
| `R_CUT` | $5.0/2.5/2.2$ | The cut-off radius. |
| `DELTA` | $0.001-0.1005$ | NVE simulation uses time step `DELTA` $= 0.001$, NVT simulation uses Metropolis move step `DELTA` $= /0.1005/$ to achieve an acceptance rate $\approx 0.5$ |
| `NBLOCKS` | $100/5000/500000$ | Number of blocks for the **blocking method**. (Equilibration: $5000$, Autocorrelation: $500000$, Analysis: $100$). |
| `NSTEPS` | $1/3000$ | Number of steps per simulation block. (Equilibration: $1$, Autocorrelation: $1$, Analysis: $3000$)) |
| `ENDINPUT` | N/A | Marker indicating the end of the input file. |

**NB**: `TEMP`, `RHO`, `R_CUT` and `DELTA` parameters are listed in the format: gas NVT-gas NVE/liquid NVT-liquid NVE/solid NVT-solid NVE.

### `properties.dat`: Measurable Properties

This file lists the properties the simulator measures and outputs.

| Parameter | Description | Applicable Simulation Type(s) | Implemented |
| :--- | :--- | :--- | :--- |
| `POTENTIAL_ENERGY` | Average potential energy per particle (using blocking) | $0, 1$ | ✅ |
| `PRESSURE` | Average pressure (using blocking) | $0, 1$ | ✅ |
| `TEMPERATURE` | Average temperature (using blocking) | $0, 1$ | ✅ |
| `KINETIC_ENERGY` | Average kinetic energy per particle (using blocking) | $0, 1$ | ✅ |
| `GOFR 100` | Radial Distribution Function using $100$ bins | $0, 1$ | ✅ |
| `ENDPROPERTIES` | N/A | Marker indicating the end of the properties file. |

### `config.xyz`

This file contains the **Cartesian coordinates** ($x, y, z$) of an FCC lattice for each particle.

---

## 3. Compilation and Execution :

This section presents the execution routine and file system management for archiving the output data generated by the **MD liquid phase simulation**. (Any other simulation follows an analogous procedure, requiring only appropriate changes to the input file described in **Configuration File Structure**). All subsequent commands must be executed from the` LSN_07/NSL_SIMULATOR/SOURCE/` directory.

**NB: Gas and solid phase simulations do not require an autocorrelation function analysis phase; therefore, these steps should be skipped, and the procedure can begin directly with the equilibration phase.**

### 3.1 Compilation, Simulator and Input Initialization

- Compile the source file:

```bash
make
```
- Copy the FCC configuration to the file read by the simulator:

```bash
cp ../INPUT/CONFIG/config.fcc ../INPUT/CONFIG/config.xyz
```

- Remove residual files from previous executions:

```bash
make remove
rm -f ../INPUT/CONFIG/conf-1.xyz
rm -f ../INPUT/seed.out
```

- Open `../INPUT/input.dat` file and copy the following input parameters:

```
SIMULATION_TYPE 0
DISTRIBUTION_TYPE 0
RESTART 0
TEMP 1.972
NPART 108
RHO 0.8
R_CUT 2.5
DELTA 0.001	
NBLOCKS 5000
NSTEPS 1
TAILS 0

ENDINPUT
```

- Open `../INPUT/properties.dat` file and copy the following property parameters:

```
POTENTIAL_ENERGY
PRESSURE
TEMPERATURE
KINETIC_ENERGY

ENDPROPERTIES
```

### 3.2 Execution (TAILS OFF)

- Execute the simulation:

```bash
./simulator.exe
```

### 3.3 Input and Output Archiviation (TAILS OFF)

- Copy the input and output directories produced by the simulation to the corresponding archive directory:

```bash
cp -r ../INPUT ../OUTPUT ../../MD_NVE/liquid/tail_corrections/off/.
```

### 3.4 Input Initialization

- Remove the generated output files:

```bash
make remove
```

- Open `../INPUT/input.dat` file and copy the following input parameters:

```
SIMULATION_TYPE 0
DISTRIBUTION_TYPE 0
RESTART 0
TEMP 1.972
NPART 108
RHO 0.8
R_CUT 2.5
DELTA 0.001	
NBLOCKS 5000
NSTEPS 1
TAILS 1

ENDINPUT
```

### 3.5 Execution (TAILS ON = EQUILIBRATION)

- Execute the simulation:

```bash
./simulator.exe
```

### 3.6 Input and Output Archiviation (TAILS ON and EQUILIBRATION)

- Copy the input and output directories produced by the simulation to the corresponding archive directories:

```bash
cp -r ../INPUT ../OUTPUT ../../MD_NVE/liquid/tail_corrections/on/.
cp -r ../INPUT ../OUTPUT ../../MD_NVE/liquid/equilibration/.
```

### 3.7 Input Initialization

- Copy the final equilibration configurations and the last used seed for the restart:

```bash
cp ../OUTPUT/CONFIG/conf-1.xyz ../INPUT/CONFIG/.
cp ../OUTPUT/CONFIG/config.xyz ../INPUT/CONFIG/.
cp ../OUTPUT/seed.out ../INPUT/.
```

- Remove the generated output files:

```bash
make remove
```

- Open the `../INPUT/input.dat` file and copy the following input parameters (**NB**: The temperature is initialized with the last average temperature from the equilibration phase):

```
SIMULATION_TYPE 0
DISTRIBUTION_TYPE 0
RESTART 1
TEMP 1.10008
NPART 108
RHO 0.8
R_CUT 2.5
DELTA 0.001	
NBLOCKS 500000
NSTEPS 1
TAILS 1

ENDINPUT
```

### 3.8 Execution (AUTOCORRELATION RUN)

- Execute the simulation:

```bash
./simulator.exe
```

### 3.9 Input and Output Archiviation (AUTOCORRELATION RUN)

- Copy the input and output directories produced by the simulation to the corresponding archive directory:

```bash
cp -r ../INPUT ../OUTPUT ../../MD_NVE/liquid/autocorrelation_run/.
```

### 3.10 Input Initialization

- Remove the generated output files:

```bash
make remove
```

- Open `../INPUT/input.dat` file and copy the following input parameters:

```
SIMULATION_TYPE 0
DISTRIBUTION_TYPE 0
RESTART 1
TEMP 1.10008
NPART 108
RHO 0.8
R_CUT 2.5
DELTA 0.001	
NBLOCKS 100
NSTEPS 3000
TAILS 1

ENDINPUT
```

- Open `../INPUT/properties.dat` file and copy the following property parameters:

```
POTENTIAL_ENERGY
PRESSURE
TEMPERATURE
KINETIC_ENERGY
GOFR 100

ENDPROPERTIES
```

### 3.11 Execution (ANALYSIS RUN)

- Execute the simulation:

```bash
./simulator.exe
```

### 3.12 Input and Output Archiviation (ANALYSIS RUN)

- Copy the input and output directories produced by the simulation to the corresponding archive directory:

```bash
cp -r ../INPUT ../OUTPUT ../../MD_NVE/liquid/run/.
```

## 4. Analysis and Results

After all data files are successfully generated, launch the analysis notebook:

```bash
jupyter-notebook LSN_Notebook_07.ipynb
```
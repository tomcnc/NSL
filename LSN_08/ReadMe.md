# Exercise 8: Variational Monte Carlo and Simulated Annealing for Quantum Systems

This document provides the necessary guidelines for executing and analyzing the code developed for the **Computational Physics Laboratory (LSN)** course.

## Prerequisites

The **`NSL_SIMULATOR`** code requires the **Armadillo C++** linear algebra library. Ensure this library is correctly **installed and linked** on your system before proceeding with compilation.

---

## 0. Introduction

The objective of this project is to implement the **Simulated Annealing (SA)** optimization algorithm to address a quantum mechanical problem. Specifically, the goal is to determine the optimal approximation of the ground state (GS) wave function for a one-dimensional particle subjected to a double-well potential defined by $V(x) = x^4 - 5x^2/2$.

As this problem lacks an **analytical closed-form solution**, a stochastic numerical approach is mandatory. We employ a trial ground state wave function $\Psi_T^{\mu, \sigma}(x)$ parameterized by $\mu$ and $\sigma$. Leveraging the **Quantum Variational Principle**,which states that the expectation value of the Hamiltonian, $\langle \hat{H} \rangle$, calculated on any arbitrary trial state has as a **lower bound** the true ground state energy ($E_0$), we aim to identify the set of variational parameters $(\mu^*, \sigma^*)$ that minimizes this expectation value.
The calculation of the energy expectation value is performed using the **Variational Monte Carlo (VMC)** method, which applies the Metropolis-Hastings algorithm to sample the probability density $|\Psi_T^{\mu, \sigma}(x)|^2$.

The following routines are implemented in this project:

- A routine for calculating the **Hamiltonian expectation value** using VMC methods and the data blocking technique.
- A routine to find the **optimal variational parameters** of the GS wave function through the SA optimization algorithm.
- A routine for the statistical analysis of the GS mean energy and for sampling the probability density $|\Psi_T^{\mu, \sigma}(x)|^2$.

---

## 1. Project Structure and Data Flow

The project is organized within the **`LSN_08`** working directory. It contains subdirectories (`ac_simulation`, `equilibration`, `final_run`, and `runs`) for all input/output data, two **Jupyter Notebooks** (one for assignment requirements, one for data analysis), the simulator code (`NSL_SIMULATOR`), and one subdirectory for aggregated plots (`plot`).

| Directory/File | Description |
| :--- | :--- |
| **`LSN_Exercises_08.ipynb`** | **Jupyter Notebook** outlining the exercise requests. |
| **`LSN_Notebook_8.ipynb`** | **Jupyter Notebook** dedicated to **data analysis and plotting**. |
| **`NSL_SIMULATOR/`** | The core **simulator** (source code and binaries). |
| **`ac_simulation`**, **`equilibration`**, **`final_run`** | Directories containing **input and output files** for VMC preparatory and final runs. |
| **`runs`** directory | Contains multiple subdirectories for executing the **SA optimization** from different starting conditions. |
| `plot/` | Directory for saving final, aggregated plots generated by the analysis notebook. |
| `images/` | Directory for supporting images, plots, or documentation visuals. |

```
LSN_08
.
├── LSN_Exercises_08.ipynb
├── LSN_Notebook_8.ipynb                # Jupyter Notebook for data analysis.
├── NSL_SIMULATOR                       # Simulator code.
│   ├── INPUT
│   ├── OUTPUT
│   └── SOURCE
├── ReadMe.md
├── ac_simulation                       # VMC Autocorrelation run files archive.
│   ├── INPUT
│   └── OUTPUT
├── equilibration                       # VMC Equilibration runs files archive.
│   ├── scenario1
│   │   ├── INPUT
│   │   └── OUTPUT
│   ├── scenario2
│   │   ├── INPUT
│   │   └── OUTPUT
│   └── scenario3
│       ├── INPUT
│       └── OUTPUT
├── final_run                           # VMC Final run files archive.
│   ├── INPUT
│   └── OUTPUT
├── images
├── plot
│   ├── GS
│   ├── SA
│   └── equilibration
└── runs                                # SA runs files archive.
    ├── run_01
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_02
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_03
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_04
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_05
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_06
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_07
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_08
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_09
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_10
    │   ├── INPUT
    │   └── OUTPUT
    ├── run_11
    │   ├── INPUT
    │   └── OUTPUT
    └── run_12
        ├── INPUT
        └── OUTPUT
```

**Note**: Sub-directories INPUT/OUTPUT and files within them are omitted for brevity.

### Execution Workflow

The analysis notebook (`LSN_Notebook_8.ipynb`) processes data from the `runs/*/OUTPUT`, `ac_simulation/OUTPUT`, `equilibration/*/OUTPUT`, and `final_run/OUTPUT` directories. Therefore, **all simulations must be successfully executed** before proceeding to the final analysis step.

---

## 2. Configuration File Structure

### `input.dat`: Simulation Parameters

This file contains configuration parameters necessary to set up the simulation.

#### A. VMC Simulations (Equilibration, AC Check, Final Run)

| Parameter | Recommended Value | Description/Notes |
| :--- | :--- | :--- |
| `SIMULATION_TYPE` | $4$ | $4$: Variational Monte Carlo (VMC). |
| `RESTART` | $0/1$ | $0$: Start from initial configuration; $1$: Resume from previous configuration (walker position). |
| `NPART` | $1$ | Number of particles. **Do not change**. |
| `DELTA` | $2.65$ | **VMC Spatial Step ($\Delta_x$):** Metropolis step width for the quantum walker position $x$. |
| `NBLOCKS` | *User Defined* | Number of blocks for the **data blocking method** (e.g., $5000$ for AC, $250$ for final run). |
| `NSTEPS` | *User Defined* | Number of VMC steps (spatial moves) per block (e.g., $1$ for AC/Equilibration, $1000$ for final run). |
| `ENDINPUT` | N/A | Marker indicating the end of the input file. |

#### B. Simulated Annealing (SA) Runs

| Parameter | Example Value | Description/Notes |
| :--- | :--- | :--- |
| `SIMULATION_TYPE` | $4$ | $4$: VMC (mode used iteratively for SA optimization). |
| `RESTART` | $0/1$ | $0$: Start new SA optimization; $1$: Resume SA from previous parameters. |
| `TEMP` | $1.5$ | **Initial SA Temperature ($T_{SA}$):** Controls the probability of accepting parameter changes that increase $E(\mu, \sigma)$. |
| `DELTA_TEMP` | 0.95 | **Cooling Rate Factor** ($\alpha$): This parameter defines the schedule for decreasing the Simulated Annealing temperature ($T_{SA}$). The next temperature step ($T_{i+1}$) is calculated by multiplying the current temperature ($T_i$) by this factor, typically a value close to, but less than, 1: $T_{i+1} = T_i \times \alpha$. A value of $\alpha = 0.95$ means the temperature drops by $5\%$ at each cooling step. Choosing a value too close to 1 results in very slow cooling (high accuracy, high computational cost), while a value too low (e.g., $\alpha < 0.9$) risks quenching the system too quickly, trapping the optimization in a local minimum. |
| `NTEMP` | $250$ | Total number of temperature cooling steps (Annealing Schedule). |
| `SA_TEMP_STEPS` | $75$ | Number of Monte Carlo steps in the **parameter space** ($\mu, \sigma$) performed at each fixed temperature. |
| `EQUIL_STEPS` | $500$ | **Micro-Equilibration Steps**: VMC spatial steps performed after a parameter update to re-thermalize the quantum walker to the new $|\Psi|^2$. |
| `NPART` | $1$ | **Do not change**. |
| `DELTA` | $2.65$ | **VMC Spatial Step ($\delta_x$):** Metropolis step width for the quantum walker. |
| `NBLOCKS` | $50$ | Number of VMC blocks used for estimating $\langle H \rangle$. |
| `NSTEPS` | $1000$ | VMC spatial steps per block used for calculating $\langle H \rangle$. |
| `NPARAM` | $2$ $\mu$ $\sigma$ | Number of variational parameters (2) and their initial starting values. |
| `DELTA_PARAM` | $\Delta_\mu$ $\Delta_\sigma$ | **Parametric Step Width:** Metropolis step widths for updating the variational parameters $(\mu, \sigma)$ during SA. |
| `ENDINPUT` | N/A | Marker indicating the end of the input file. |

### `properties.dat`: Measurable Properties

This file lists the properties the simulator measures and outputs.

| Parameter | Description | Applicable Simulation Type(s) | Implemented |
| :--- | :--- | :--- | :--- |
| `LOCAL_ENERGY` | Average GS energy (using blocking) | $4$ | ✅ |
| `LOCAL_ENERGY_STEP` | GS energy per VMC step | $4$ | ✅ |
| `WALKER_POSITION` | Prints VMC walker position at each VMC step | $4$ | ✅ |
| `NO_PRINT_VMC` | Disables Blocking Method output printing during SA runs (to reduce execution time and file size). | $4$ | ✅ |
| `NO_PRINT_ACC` | Disables VMC acceptance ratio output printing during SA runs. | $4$ | ✅ |
| `GS 100` | Sampling of the squared GS wave function modulus using $100$ bins (histogram). | $4$ | ✅ |
| `ENDPROPERTIES` | N/A | Marker indicating the end of the properties file. |

### `config.xyz`

This file contains the **Cartesian coordinates** ($x, y, z$) of the quantum walker's starting position.

---

## 3. Compilation and Execution

This section presents the execution routine and file system management for archiving the output data generated during **simulation**. All subsequent commands must be executed from the `LSN_08/NSL_SIMULATOR/SOURCE/` directory.


### 3.1 Compilation

- Compile both source file (`SA.cpp` and `NSL_SIMULATOR.cpp`):

```bash
make
```
- Copy the starting configuration file to the file read by the simulator:

```bash
cp ../INPUT/CONFIG/config.vmc ../INPUT/CONFIG/config.xyz
```

- Remove residual files from previous executions:

```bash
make remove
rm -f ../INPUT/CONFIG/conf-1.xyz
rm -f ../INPUT/seed.out
```

### 3.2.1 Input Parameters Initialization (VMC)

- Open `../INPUT/input.dat` file and copy the following input parameters:

```
SIMULATION_TYPE 4
RESTART 0
NPART 1
DELTA 2.65
NBLOCKS 5000/100
NSTEPS 1/1000
NPARAM 2 0.8 0.6

ENDINPUT
```

- Open `../INPUT/properties.dat` file and copy the following property parameters:

```
LOCAL_ENERGY
GS 100

ENDPROPERTIES
```

### 3.2.2 Execution (VMC)

- Execute the simulation:

```bash
./simulator.exe
```

### 3.2.3 Input and Output Archiviation (VMC)

- Copy the input and output directories produced by the simulation to the corresponding archive directory (e.g. `equilibration`, `ac_simulation`, `final_run`):

```bash
cp -r ../INPUT ../OUTPUT ../../equilibration/scenario_*/.
```

### 3.3.1 Input Initialization (SA)

Repeat the following instructions to perform different SA simulations with varying starting parameters:

- Remove the generated output files:

```bash
make remove
```

- Open `../INPUT/input.dat` file and copy the following input parameters:

```
SIMULATION_TYPE 4
RESTART 0
TEMP 1.5
DELTA_TEMP 0.95
NTEMP 250
SA_TEMP_STEPS 75
EQUIL_STEPS 500
NPART 1
DELTA 2.65
NBLOCKS 50
NSTEPS 1000
NPARAM 2 0.5 0.6
DELTA_PARAM 0.1 0.1

ENDINPUT
```

- Open `../INPUT/properties.dat` file and copy the following property parameters:

```
NO_PRINT_VMC
NO_PRINT_ACC
LOCAL_ENERGY

ENDPROPERTIES
```

### 3.3.2 Execution (SA)

- Execute the simulation:

```bash
./SA.exe
```

### 3.3.3 Input and Output Archiviation (SA)

- Copy the input and output directories produced by the simulation to the corresponding archive directories:

```bash
cp -r ../INPUT ../OUTPUT ../../runs/run_*/.
```

---

## 4. Analysis and Results

After all data files are successfully generated, launch the analysis notebook:

```bash
jupyter-notebook LSN_Notebook_08.ipynb
```